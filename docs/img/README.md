# Screenshots for the main README

Drop these four PNGs in this folder and they appear in the README. Names must
match exactly.

| File | What it should show |
|---|---|
| `1-extension.png` | The Extensions tab, with **Extension Pack for Java** and its Install button |
| `2-open-file.png` | `AdaLovelace.java` open, with the exercise comment at the top visible |
| `3-run.png` | The **Run** link above `main`, and `Ada Lovelace` printed in the panel below |
| `4-tests.png` | The Testing tab, with `AdaLovelaceTest` passing |

Crop to the part being described, so the detail is readable on a phone.
